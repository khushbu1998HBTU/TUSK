CamelCase


nt camelcase(string s) {
    int count=1;
    int l= s.length();
    for(int i=0;i<l;i++)
    {
        int c=s[i];
        if(islower(c))
        continue;
        else
        count++;
    }
return count++;

}
